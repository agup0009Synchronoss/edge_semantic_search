# Superset of research/vitb32_benchmark/requirements.txt needed for calibration.
# Reuse the existing venv_clip (Python 3.11) — these are already present there
# except where noted. Pins match the Gradio app to keep the ONNX encoders
# byte-identical.
numpy==2.4.6
onnx==1.22.0
onnxruntime==1.27.0
onnxruntime_extensions==0.15.0
pillow==12.2.0
requests==2.34.2
tqdm>=4.66
scikit-learn>=1.4          # only used for cross-checks / optional metrics
pyarrow>=15.0              # calibration_table.parquet (optional; JSON always written)
